// Pure transform function: takes a source DOM element + config, returns a new element or null.
// No direct DOM querying or side effects beyond creating/returning the new element.

import type { ResolvedConfig } from './types.js';

export function transformElement(
  _element: Element,
  _config: ResolvedConfig,
): HTMLElement | null {
  // TODO: Implement in Phase 3
  return null;
}
