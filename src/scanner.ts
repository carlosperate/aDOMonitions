// DOM scanning logic: queries qualifying elements within the root scope
// and delegates to the transform function per trigger style.

import type { ResolvedConfig } from './types.js';

export function scan(_config: ResolvedConfig): void {
  // TODO: Implement in Phase 5
}
